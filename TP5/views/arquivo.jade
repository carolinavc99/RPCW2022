extends layout

block content
  h1 
    a(href="/arquivo") Lista de Músicas
  h2 25 de março de 2022
  if (typeof(flag) !== 'undefined')
    h3 A filtrar por #{flag}

  table
    tr
        th.id ID
        th.dur DURAÇÃO
        th.file FILE
        th.filetype FILE TYPE
        th.local LOCALIDADE
        th.mus MÚSICO
        th.obs OBSERVAÇÕES
        th.obsfiles OBS FILES
        th.prov PROVÍNCIA
        th.tit TÍTULO
    each musica in arquivo
        tr
            td
                a(href="/arquivo/id/" + musica.id) #{musica.id} 🔍
            td= musica.duracao
            td= musica.file 
            td= musica.fileType
            td 
                a(href="/arquivo/local/" + musica.local) #{musica.local} 🔍
            td= musica.musico 
            td
                - if (typeof(musica.obs) !== 'undefined'){
                    p= musica.obs
                -}
            td
                - if (typeof(musica.obsFiles) !== 'undefined'){
                    each file in musica.obsFiles
                        p= file.file
                        p= file.fileType
                -}
            td
                a(href="/arquivo/prov/" + musica.prov) #{musica.prov} 🔍
            td= musica.tit
